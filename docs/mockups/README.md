# UI interpretation mockups

Visual partials for the simulator's UI requirements (`docs/sim_ui.md`,
`docs/sim_spec.md`) — built to check the *reading* of the spec before any UI
code exists, not to pin down final visuals.

## Naming

`<requirement(s)>-<what-it-is>` — each file names the main `sim_ui.md`
requirement(s) it addresses and what kind of mock it is (diagram / panel /
legend). One concern per file, one scheme.

| File | What it is | Requirements |
|---|---|---|
| `U6-diagram` | Holistic diagram: wiring band, zone rows off the manifold, supply chain. State = pump on, Z2 watering. | U6 · R10·R11·R13·R14 · U2·U7–U13·U16 |
| `U8-diagram-with-panel` | The diagram with the inspector panel docked beside it — the integrated whole-app view. | U8 · U6·U7·U18·U14·U22 |
| `U18-panel-overlay` | Tap an item → panel **overlays** the (faded) diagram: live state, catalog range, item controls. | U18 · U8·U9·U17·U19·U22 |
| `U18-panel-docked` | Tap an item → panel **docks beside** the diagram, which stays the canvas. | U18 · U8·U14·U15·U17·U19·U20 |
| `U18-overlay-transition` | The overlay before/after: nothing selected (diagram full-screen) → item tapped (panel slides in over it). | U18 · U7·U8·U17 |
| `U17-U18-panel-anatomy` | The panel's internal structure: system summary, live values vs catalog range, item controls. | U17·U18 · U9·U19 |
| `U8-panel-in-context` | Rough study: the docked panel sitting beside the full diagram on desktop. | U8·U18 · U6·U17 |
| `U2-panel-mobile` | The docked panel on a phone-portrait screen (diagram left, panel right). | U2·U18 · U8·U17 |
| `U8-layout-options` | The U8 phone question: numbers in a split (diagram top / panel below) vs a tap-up sheet. | U8 · U2·U3 |
| `U14-U15-head-glyph` | How much one head can carry: coverage wedge, throw/flow/precip labels, type, status. | U14·U15 · U9·U22 |
| `U12-U16-legend` | Encoding legend: how each value is drawn — flow, pressure, status icons, coverage wedge, live/broken wiring. | U12–U16 · U9·U21·R12 |

## Layout

SVG **sources** live in `svg/`; the rendered **PNGs** (what you view inline)
sit at the top level next to this README, same basename.

## Regenerating

Three sheets — `U6-diagram`, `U18-panel-overlay`, `U12-U16-legend` — are
generated by `gen.py`; the rest are hand-authored SVG in `svg/` (edited
directly).

```sh
python3 gen.py        # (re)writes the generated SVG sources into svg/
python3 svg2png.py    # renders every svg/*.svg to a same-named *.png here
```

`svg2png.py` is a standard SVG→PNG converter (`pip install cairosvg`); it keeps
every committed PNG in sync with its SVG source, hand-authored ones included.

## Open choices left for the user (not decided here)

Flow magnitude (thickness vs dash-speed vs both); pressure (fill saturation vs
number badge vs both); coverage wedges (always-on vs selected-head only); side
panel overlay vs docked; on a phone, split vs tap-up sheet (U8).
</content>
</invoke>
